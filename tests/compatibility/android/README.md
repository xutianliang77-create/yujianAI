# Android compatibility target (deferred)

Use the official LiveKit Android SDK and keep `livekit` package/protocol names unchanged. Required
flows are join, audio publish/subscribe, Data/RPC, network fallback and permission denial. Run only
in the Beelink acceptance program or a declared device runner.

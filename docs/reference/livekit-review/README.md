# LiveKit研究基线

本目录保存语见AI采用 LiveKit 兼容路线的官方资料、版本快照和研究结论。

这些材料：

- 可以用于确认协议、媒体、SIP、Ingress/Egress、Agent 和安全设计。
- 不代表语见AI依赖旧项目源码。
- 不包含旧项目密钥、配置、数据库、模型或构建产物。
- 后续采用 LiveKit 版本时仍需针对固定 tag 和镜像重新扫描。
- 不能用未固定的网页“最新版本”替代可复现的 commit/tag manifest。

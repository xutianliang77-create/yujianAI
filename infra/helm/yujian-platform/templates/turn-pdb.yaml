{{- if and .Values.features.turnEnabled (gt (int .Values.turn.replicas) 1) }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "yujian-platform.fullname" . }}-turn
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: turn
{{- end }}

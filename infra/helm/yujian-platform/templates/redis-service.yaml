apiVersion: v1
kind: Service
metadata:
  name: {{ include "yujian-platform.fullname" . }}-redis
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/component: redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis

{{ if .Values.features.mediaOpsEnabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "yujian-platform.fullname" . }}-media-ops
spec:
  selector:
    app.kubernetes.io/component: media-ops
  ports:
    - name: https
      port: {{ .Values.service.mediaOpsPort }}
      targetPort: https
{{ end }}

{{ if not .Values.platformRuntime.modulePath }}
{{ fail "platformRuntime.modulePath must be set for production platform-api" }}
{{ end }}
{{ if and (eq .Values.dataServices.mode "external-ha") (or (not .Values.platformRuntime.postgresUrlSecretKey) (not .Values.platformRuntime.redisUrlSecretKey)) }}
{{ fail "external-ha dataServices require platformRuntime postgresUrlSecretKey and redisUrlSecretKey" }}
{{ end }}
{{ if and (eq .Values.dataServices.mode "external-ha") (eq (len .Values.dataServices.externalEgressCidrs) 0) }}
{{ fail "external-ha dataServices require at least one NetworkPolicy externalEgressCidrs entry" }}
{{ end }}
{{ if eq (len .Values.platformRuntime.kmsEgressCidrs) 0 }}
{{ fail "production platform-api requires at least one explicit KMS egress CIDR" }}
{{ end }}
{{ if and .Values.features.turnEnabled (or (eq (len .Values.turn.publicUrls) 0) (not .Values.turn.secretRef)) }}
{{ fail "turnEnabled requires turn.publicUrls and a KMS turn.secretRef" }}
{{ end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "yujian-platform.fullname" . }}-platform-api
spec:
  replicas: {{ .Values.replicaCount.platformApi }}
  selector:
    matchLabels:
      app.kubernetes.io/component: platform-api
  template:
    metadata:
      labels:
        app.kubernetes.io/component: platform-api
    spec:
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: {{ .Values.security.runAsUser }}
        runAsGroup: {{ .Values.security.runAsGroup }}
        fsGroup: {{ .Values.security.fsGroup }}
        seccompProfile: { type: RuntimeDefault }
      {{ with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{ toYaml . | nindent 8 }}
      {{ end }}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/component: platform-api
      containers:
        - name: platform-api
          image: {{ .Values.image.platformApi | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: [ALL] }
            readOnlyRootFilesystem: {{ .Values.security.readOnlyRootFilesystem }}
          ports:
            - name: http
              containerPort: {{ .Values.service.platformApiPort }}
          env:
            - name: NODE_ENV
              value: production
            - name: PLATFORM_API_PORT
              value: {{ .Values.service.platformApiPort | quote }}
            - name: PLATFORM_API_HOST
              value: 0.0.0.0
            - name: YUJIAN_RTC_TELEMETRY_RETENTION_DAYS
              value: {{ .Values.observability.rtcTelemetryRetentionDays | quote }}
            - name: YUJIAN_PLATFORM_CREDENTIALS_JSON
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: {{ .Values.secrets.platformCredentials }}
            - name: YUJIAN_PLATFORM_ADMIN_CREDENTIAL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: {{ .Values.secrets.adminCredential }}
            - name: YUJIAN_RTC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: {{ .Values.secrets.rtcApiKey }}
            - name: YUJIAN_RTC_API_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: {{ .Values.secrets.rtcApiSecret }}
            - name: YUJIAN_RTC_PRIMARY_URL
              value: {{ required "rtc.primaryWsUrl must be set" .Values.rtc.primaryWsUrl | quote }}
            {{ if .Values.rtc.secondaryWsUrl }}
            - name: YUJIAN_RTC_SECONDARY_URL
              value: {{ .Values.rtc.secondaryWsUrl | quote }}
            {{ end }}
            {{ if .Values.features.rtcCapacityEnabled }}
            - name: YUJIAN_REQUIRE_RTC_CAPACITY
              value: "true"
            - name: YUJIAN_RTC_CAPACITY_CREDENTIAL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: {{ .Values.secrets.rtcCapacityCredential }}
            {{ end }}
            {{ if .Values.features.turnEnabled }}
            - name: YUJIAN_REQUIRE_TURN_CREDENTIALS
              value: "true"
            - name: YUJIAN_TURN_URLS
              value: {{ .Values.turn.publicUrls | toJson | quote }}
            - name: YUJIAN_TURN_SECRET_REF
              value: {{ .Values.turn.secretRef | quote }}
            {{ end }}
            {{ if .Values.platformRuntime.modulePath }}
            - name: YUJIAN_PLATFORM_RUNTIME_MODULE
              value: {{ .Values.platformRuntime.modulePath | quote }}
            {{ end }}
            {{ if .Values.platformRuntime.postgresUrlSecretKey }}
            - name: YUJIAN_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: {{ .Values.platformRuntime.postgresUrlSecretKey }}
            {{ end }}
            {{ if .Values.platformRuntime.redisUrlSecretKey }}
            - name: YUJIAN_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: {{ .Values.platformRuntime.redisUrlSecretKey }}
            {{ end }}
            {{ if .Values.features.mediaOpsEnabled }}
            - name: YUJIAN_MEDIA_OPS_URL
              value: {{ required "mediaOps.baseUrl must be set when mediaOpsEnabled" .Values.mediaOps.baseUrl | quote }}
            - name: YUJIAN_MEDIA_OPS_CREDENTIAL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.existingSecret }}
                  key: {{ .Values.secrets.mediaOpsCredential }}
            {{ end }}
          resources:
            {{- toYaml .Values.resources.platformApi | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3

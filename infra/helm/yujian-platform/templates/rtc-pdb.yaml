{{- if and .Values.features.rtcEnabled (gt (int .Values.replicaCount.rtc) 1) }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "yujian-platform.fullname" . }}-rtc
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: rtc
{{- end }}
